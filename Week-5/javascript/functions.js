function name(parameter1, parameter2, parameter3) {
    // code to be executed
  }

  function add_numbers(number1,number2) {
    return number1 + number2
    // code to be executed
  }

  let x = add_numbers(4, 3);


  